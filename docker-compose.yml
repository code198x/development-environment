version: '3.8'

services:
  c64-dev:
    build: .
    image: code198x/commodore-64:latest
    container_name: c64-development
    volumes:
      - .:/workspace
      # Persist bash history
      - bash-history:/root
    working_dir: /workspace
    stdin_open: true
    tty: true
    # X11 forwarding for VICE emulator GUI (optional)
    environment:
      - DISPLAY=${DISPLAY:-:0}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
    network_mode: host

volumes:
  bash-history:
